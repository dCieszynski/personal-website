export type TLink = {
  to: string;
  title: string;
};

export type TProjectCardContext = {
  name: string;
  description: string;
  repository: string;
  live: string;
  image: string;
  alt: string;
};
